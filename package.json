{
  "name": "relnote-compare",
  "version": "1.0.0",
  "description": "CLI to extract release notes from Excel files into JSON",
  "scripts": {
    "extract": "node main.js mode=local",
    "download": "node main.js mode=download",
    "install:browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "node-xlsx": "^0.24.0",
    "playwright": "^1.61.0"
  }
}
